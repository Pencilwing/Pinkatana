/// @description  move_collide(direction);
/// @function  move_collide
/// @param direction
function move_collide(argument0) {

	//if collision is in x, zero xSpeed, if it is in y, zero ySpeed
	if (argument0 == "x")
	{
	xSpeed = 0;
	}else{
	ySpeed = 0;
	}


}
